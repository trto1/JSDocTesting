/**
 * Function prints the inputted string n many times
 * @param {string} something - the color of the car
 * @param {int} n - how many times
 *
*/
function printSomething (something, n) {
    for (let i = 1; i<= n; i++) {
        console.log (something);
    }
}

printSomething ("dana", 2);
